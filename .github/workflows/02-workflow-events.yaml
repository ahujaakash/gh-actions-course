name: 01-Workflow Events

on:
  - push
  - workflow_dispatch
  - pull_request

jobs:
    echo:
      runs-on: ubuntu-latest
      steps:
        - name: Show the Trigger
          run: echo "Hello, World!!!"
